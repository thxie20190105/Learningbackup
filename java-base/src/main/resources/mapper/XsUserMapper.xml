<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.xigua.study.mybatis.mapper.XsUserMapper">

    <select id="select" resultType="org.xigua.study.mybatis.entity.XsUser">
        select s.user_name as userName,
               s.user_age  as userAge,
               s.user_id   as userId
        from xs_user s;

    </select>


    <select id="test">



    </select>

</mapper>
